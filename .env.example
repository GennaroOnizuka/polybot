# === CLOB Authentication (docs: https://docs.polymarket.com/developers/CLOB/authentication) ===
# L1 = private key (firma messaggi). L2 = API credentials (apiKey, secret, passphrase).
# Le credenziali L2 non si creano da Settings: si ottengono con create_or_derive_api_creds (L1).
# Se vuoti sotto, il bot le deriva alla prima run e le stampa per copiarle qui.
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# L1: chiave privata del wallet che firma (senza 0x). MetaMask = EOA che controlla il proxy.
PRIVATE_KEY=your_private_key_here

# Builder Key (opzionale, per Builder Program; non usata per ordini CLOB)
BUILDER_KEY=

# Signature Type + Funder (doc: "Signature Types and Funder")
# 0 = EOA (MetaMask solo; funder = EOA)
# 1 = POLY_PROXY (Magic Link / email)
# 2 = GNOSIS_SAFE (proxy wallet più comune; funder = indirizzo mostrato su polymarket.com)
SIGNATURE_TYPE=2
# Per type 2: indirizzo wallet mostrato su polymarket.com/settings (proxy = funder che detiene i fondi)
POLY_SAFE_ADDRESS=0xYourPolymarketProxyAddress

# Proxy DataImpulse HTTP - porta 823. Per ordini si provano più paesi (user__cr.ch, no, se, nl, dk)
# PROXY_URL=http://USER:PASSWORD@gw.dataimpulse.com:823
# Oppure: PROXY_HOST, PROXY_PORT, PROXY_USER, PROXY_PASS (o PROXY_PASSWORD)
# PROXY_ALLOW_NON_CH=1   # avvia anche se proxy non è in Svizzera (CH)
# POLYBOT_DEBUG_PROXY=1  # debug proxy/httpx
# SKIP_PROXY_TEST=1      # salta test proxy all'avvio

# Event to monitor (solo questo evento quando il bot parte)
# Bitcoin Up or Down 5m: https://polymarket.com/event/btc-updown-5m
MONITOR_EVENT_SLUG=btc-updown-5m

# Comportamento: 1 (default) = monitora, fa 1 scommessa da 1$ sul lato con % più alta e si ferma
# 0 = monitoraggio continuo (quote + opportunità senza fermarsi)
SINGLE_BET=1

# Risk Management
MAX_POSITION_SIZE=0.05
# Maximum position size as fraction of portfolio (5%)

# Trading Parameters
MIN_PROFIT_MARGIN=0.02
# Minimum profit margin for arbitrage (2%)
